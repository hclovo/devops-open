apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "prompt-minder-helm.fullname" . }}-test-connection"
  labels:
    {{- include "prompt-minder-helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "prompt-minder-helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
