apiVersion: v1
kind: Service
metadata:
  name: {{ include "prompt-minder-helm.fullname" . }}
  labels:
    {{- include "prompt-minder-helm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "prompt-minder-helm.selectorLabels" . | nindent 4 }}
